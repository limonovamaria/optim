
Необходимо реализовать:
1. Получаем данные из json (k_targ)
2. Считаем количество параметров для формирования симплекса
3. Как засунуть коэффициенты в функцию?
   Функция обращается к серверу БД?
   Нет, держит в кэше, обращается к серверу раз в какое-то время
4. Во всех операциях отражения и т.п. нужно учитывать тип переменных для a, b, c, d
5. Рассмотреть вариант штрафа через умножение функции, а не через сумму
6. ИДЕЯ: степень (и коэффициент) штрафа зависит от нагруженности сервера, при большой нагрузке выдаётся менее точный результат


Выводы:
1. Резкое штрафование не работает, поэтому нельзя резко обрубать плохие результаты на границе
